import "@/styles/elementor-reimbursement-bundle.css";
import "@/styles/elementor-kit-fallback.css";

/** Elementor CSS is bundled via `@/styles/elementor-reimbursement-bundle.css` (loads in `<head>`). */
export default function BehavioralHealthReimbursementRatesLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children;
}
