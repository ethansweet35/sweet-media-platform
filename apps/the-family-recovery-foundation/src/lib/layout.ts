/** Pull page content under the fixed header — use on Layout main wrapper only. */
export const LAYOUT_NAV_OVERLAP = "-mt-[5.25rem] md:-mt-[5.75rem]";

/**
 * Content inset below the fixed pill navbar.
 * Must include LAYOUT_NAV_OVERLAP so effective clearance matches pt-24/28/32 (96/112/128px).
 */
export const PAGE_TOP_NAV_PADDING = "pt-[11.75rem] md:pt-[13.25rem] lg:pt-[14.25rem]";
